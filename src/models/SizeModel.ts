class SizeModel {
    sizeID : number;
    sizeName: string;

    constructor(sizeID : number, sizeName: string) {
        this.sizeID = sizeID;
        this.sizeName = sizeName;
    }
}

export default SizeModel;